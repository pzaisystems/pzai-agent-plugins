{
  "name": "claude-channel-precontext",
  "version": "0.2.0",
  "description": "Claude Code MCP channel plugin for PreContext messaging platform",
  "license": "Apache-2.0",
  "type": "module",
  "bin": "./server.ts",
  "scripts": {
    "start": "bun install --no-summary && bun server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
